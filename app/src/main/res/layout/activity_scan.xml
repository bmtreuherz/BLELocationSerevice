<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <Button
        android:id="@+id/startScanButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:layout_centerHorizontal="true"
        android:text="Start Scan"/>

    <TextView
        android:id="@+id/scanStatusTV"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="50dp"
        android:layout_below="@+id/startScanButton"
        android:layout_centerHorizontal="true"
        android:text="Status: Not Connected"/>

    <TextView
        android:id="@+id/serverMessageTV"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="50dp"
        android:layout_below="@+id/scanStatusTV"
        android:layout_centerHorizontal="true"
        android:text="Message: N/A"/>

    <EditText
        android:id="@+id/messageET"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:layout_below="@+id/serverMessageTV"
        android:layout_centerHorizontal="true"
        android:text="Message to the server"/>

    <Button
        android:id="@+id/sendToServerButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:layout_below="@+id/messageET"
        android:layout_centerHorizontal="true"
        android:text="Send Message to Server"/>

</RelativeLayout>
